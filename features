Remove ability to set ROOT_DIR dynamically
Clear search leaves the selected item selected
copy results from search to folder
Support multiple roots
Delete the last one causes an error
Tagged timestamp (with tz)
Advanced search - add NOT for a group. OR between groups
--category define extensions
Searchable "Add tag" textbox (names really, any fast libraries in the backend?)
--Index on Asset.path
--Undo deletes up to 10
--Take category, extension as req params when root_dir is specified
--Support mov and mp4 at the same time (simiarly heic and jpg)
--Not tagged button
--Navigation panel stays at the bottom
--Uinque constraings (Same named tags can be in two diff groups)
Scoring based on time lingering on media, tag numbers, tag scores, etc
Date of media creation (modification?), search by date range
--Move to trash
--Carry next
--Dropdown tag group
Auto video length detect (but we already know from the player!)
Auto faces recognition
Auto interactions classifications
Tags integrity script (python or SQL)
Alert if manadatory tag is not filled on page exit
